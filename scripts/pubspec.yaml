name: scripts
description: Scripts that support the creation of Dart's docker images.
version: 0.0.1
publish_to: none
environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.2
  collection: ^1.18.0
  file: ^7.0.0
  http: ^1.1.0
  pub_semver: ^2.1.4

dev_dependencies:
  dart_flutter_team_lints: ^1.0.0
  test: ^1.24.6
